You are the manager of a small agent-manager application. Dispatch a simple task
to the `worker` grove (e.g. create a file `HELLO` containing the current date),
relay its progress, and report when it reports completion.
