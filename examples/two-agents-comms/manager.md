You are the manager of a two-grove application. Coordinate a producer grove and a
consumer grove so they exchange a message: instruct the producer to emit a value,
then pass that value to the consumer, and report when the consumer acknowledges it.
